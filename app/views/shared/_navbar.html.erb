<%# Main container %>
<div class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">

  <%# Logo button %>
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand" do %>
      <%= image_tag "brand_logoo.jpg", class: "img-fluid", style: "max-height: 40px;" %>
    <% end %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
      </button>
  </div>

  <%# turbo search bar code %>
<%# <%= form_with method: :get, data: { turbo_frame: :dropdown_search_results }, name: "unique_name_#{SecureRandom.hex}"  do |form| %>
    <%# <div class="search-input">
      <%= form.text_field :search, placeholder: "Search Manga", class: "search-form", data: { controller: 'search', action: 'keyup->search#submitForm' }, autocomplete: "new-password" %>
      <%# <button type="submit" class="search-button"> %>
        <%# <i class="fa-solid fa-search"></i> %>
      <%# </button> %>
    <%# </div> %>
  <%# <% end %>

<%# PG-search bar %>
<% <div class="collapse navbar-collapse" id="navbarSupportedContent" ></div>
  <div class="d-flex flex-row w-100 justify-content-between">
    <ul class="navbar-nav">
      <ul class="navbar-nav">
        <%= form_with url: mangas_path, method: :get, local: true, html: { class: "d-flex search-form" } do |form| %>
          <% <%= form.text_field :search, placeholder: "Search Manga", class: "form-control me-2",
            data: { target: "search.query",
            action: "keyup->search#search_manga" } %>
          <% <%= form.submit "Search", class: "btn btn-outline-primary" %>
        <% <% end %>
      <% </ul>
    <ul class="navbar-nav"></ul>
  </div>
</div>

  <%# Displays username if logged in %>
  <ul class="navbar-nav">
    <% if user_signed_in? %>
      <li class="nav-item">
        <%= link_to "Hi #{current_user.username}!", "#", class: "nav-link" %>
      </li>

      <%# My dashboard button %>
      <li class="nav-item">
        <%= link_to "My Dashboard", dashboard_index_path, class: "nav-link" %>
      </li>
          <%# button to user "message" - currently fake %>
          <li class="nav-item position-relative me-3 border border-primary">
            <%= link_to "Message", "#", class: "nav-link pe-3 border border-primary"%>
            <span class="position-absolute top-50 start-100 translate-middle p-2 bg-info badge rounded-pill border border-primary">1
              <span class="visually-hidden">New alerts</span>
            </span>
          </li>

          <%# javascript - User Avatar dropdown box %>
          <li class="nav-item dropdown">
            <a href="#" class="avatar" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <% if current_user.avatar.present? %>
                <%= cl_image_tag current_user.avatar.key, class: "avatar rounded-circle", alt: "dropdown menu", style: "max-height: 40px;" %>
              <% else %>
                <i class="avatar-user border border-primary fa-solid fa-user"></i>
              <% end %>
            </a>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%# example: <%= link_to "Dashboard", dashboard_index_path, class: "dropdown-item" %>
              <%# example: link_to "Action", "#", class: "dropdown-item" %>
              <%= link_to "Edit Profile", edit_user_registration_path, class: "dropdown-item" %>
              <%= link_to "Setting", "#", class: "dropdown-item" %>
              <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
            </div>
          </li>
        <% else %>
          <%# Button to Login in %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
  </ul>
  </div>
</div>

<%# TODO: MARY - Style Uncomment/move to CSS %>
<%# <style>
  .navbar {
    font-family: 'Roboto', sans-serif;
    color: #A8EACE;
  }

  .navbar-brand {
    font-weight: bold;
  }

  .nav-link {
    color: #A8EACE;
  }

  .nav-link:hover {
    color: #ff6900;
  }

  .avatar {
    max-height: 40px;
  }

  .search-form {
    margin-right: 1rem;
  }

  .container-fluid {
    color: #A8EACE;
  }
</style> %>
